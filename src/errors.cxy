module errors

pub exception PgError(msg: String) => msg != null ? msg.str() : "postgres error"

pub exception PgConnectionError(msg: String) => msg != null ? msg.str() : "postgres connection error"

pub exception PgQueryError(msg: String) => msg != null ? msg.str() : "postgres query error"
